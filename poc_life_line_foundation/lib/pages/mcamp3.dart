import 'package:flutter/material.dart';

class upcoming3 extends StatelessWidget {
  const upcoming3({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('details'),
        ),
        body: Center(child: Text('this is camp3')));
  }
}
