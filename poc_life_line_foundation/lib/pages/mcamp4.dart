import 'package:flutter/material.dart';

class upcoming4 extends StatelessWidget {
  const upcoming4({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('details'),
        ),
        body: Center(child: Text('this is camp4')));
  }
}
